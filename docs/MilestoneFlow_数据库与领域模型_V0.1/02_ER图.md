# 《MilestoneFlow Pilot MVP V0.1 ER 图》

## 1. 图例

- `||--o{`：一对多。
- `||--o|`：一对零或一。
- 标注 `projection` 的表为可重建读模型，不是商业事实唯一来源。
- 所有租户业务关系在物理数据库中同时使用 `workspace_id` 组合外键；图中为可读性省略部分重复连线。

## 2. 核心实体 ER 图

```mermaid
erDiagram
    APP_USER ||--o{ WORKSPACE_MEMBERSHIP : joins
    WORKSPACE ||--o{ WORKSPACE_MEMBERSHIP : contains
    WORKSPACE ||--o{ CLIENT : owns
    WORKSPACE ||--o{ PROJECT : isolates
    CLIENT ||--o{ PROJECT : commissions
    PROJECT ||--o{ MILESTONE : contains
    PROJECT ||--o{ ACTION_ITEM_PROJECTION : derives
    PROJECT ||--o{ AUDIT_EVENT : records
    PROJECT ||--o{ FEEDBACK_PROJECTION : aggregates

    APP_USER {
      uuid id PK
      varchar email_normalized UK
      varchar display_name
      varchar status
      bigint version
      timestamptz created_at
      timestamptz updated_at
    }
    WORKSPACE {
      uuid id PK
      varchar name
      varchar slug UK
      char default_currency
      varchar timezone
      varchar status
      bigint version
    }
    WORKSPACE_MEMBERSHIP {
      uuid id PK
      uuid workspace_id FK
      uuid user_id FK
      varchar role
      varchar status
      timestamptz joined_at
      timestamptz ended_at
    }
    CLIENT {
      uuid id PK
      uuid workspace_id FK
      varchar name
      varchar status
      varchar primary_email
      bigint version
    }
    PROJECT {
      uuid id PK
      uuid workspace_id FK
      uuid client_id FK
      uuid current_baseline_id FK
      varchar project_no
      varchar name
      char currency
      varchar status
      date target_end_date
      bigint version
    }
    MILESTONE {
      uuid id PK
      uuid workspace_id FK
      uuid project_id FK
      int sequence_no
      varchar title
      varchar status
      date due_date
      bigint version
    }
    ACTION_ITEM_PROJECTION {
      uuid id PK
      uuid workspace_id FK
      uuid project_id FK
      varchar source_type
      uuid source_id
      varchar action_type
      varchar status
      varchar priority
      timestamptz due_at
    }
    AUDIT_EVENT {
      uuid id PK
      uuid workspace_id FK
      uuid project_id FK
      varchar aggregate_type
      uuid aggregate_id
      varchar event_type
      varchar actor_type
      uuid request_id
      timestamptz occurred_at
      jsonb metadata
    }
    FEEDBACK_PROJECTION {
      uuid id PK
      uuid workspace_id FK
      uuid project_id FK
      varchar source_type
      uuid source_id
      varchar feedback_type
      text content
      varchar author_type
      timestamptz occurred_at
    }
```

## 3. 商业闭环扩展 ER 图

```mermaid
erDiagram
    WORKSPACE ||--o{ PROJECT : owns
    PROJECT ||--o{ QUOTE_DRAFT : has
    QUOTE_DRAFT ||--o{ QUOTE_VERSION : publishes
    QUOTE_VERSION ||--o| QUOTE_DECISION : receives
    PROJECT ||--o{ COMMERCIAL_BASELINE : snapshots
    COMMERCIAL_BASELINE ||--o{ BASELINE_ITEM : contains

    PROJECT ||--o{ MILESTONE : contains
    MILESTONE ||--o{ DELIVERY_DRAFT : prepares
    DELIVERY_DRAFT ||--o{ DELIVERY_VERSION : submits
    DELIVERY_VERSION ||--o| ACCEPTANCE_DECISION : receives
    DELIVERY_VERSION ||--o{ FILE_REFERENCE : references
    FILE_ASSET ||--o{ FILE_REFERENCE : linked_by

    PROJECT ||--o{ CHANGE_DRAFT : has
    CHANGE_DRAFT ||--o{ CHANGE_VERSION : publishes
    CHANGE_VERSION ||--o| CHANGE_DECISION : receives
    COMMERCIAL_BASELINE ||--o{ CHANGE_VERSION : based_on

    PROJECT ||--o{ RECEIVABLE : generates
    RECEIVABLE ||--o{ PAYMENT_RECORD : settles
    RECEIVABLE ||--o{ REMINDER : reminds

    QUOTE_VERSION ||--o{ PUBLIC_LINK : exposes
    DELIVERY_VERSION ||--o{ PUBLIC_LINK : exposes
    CHANGE_VERSION ||--o{ PUBLIC_LINK : exposes
    PUBLIC_LINK ||--o{ PUBLIC_SESSION : exchanges
    PUBLIC_LINK ||--o{ PUBLIC_ACCESS_LOG : logs

    WORKSPACE ||--o{ IDEMPOTENCY_RECORD : scopes
    WORKSPACE ||--o{ EVENT_PUBLICATION : emits
    EVENT_PUBLICATION ||--o{ EMAIL_TASK : creates
    EMAIL_TASK ||--o{ EMAIL_ATTEMPT : attempts

    QUOTE_DRAFT {
      uuid id PK
      uuid workspace_id FK
      uuid project_id FK
      varchar status
      bigint version
    }
    QUOTE_VERSION {
      uuid id PK
      uuid workspace_id FK
      uuid quote_draft_id FK
      uuid project_id FK
      int version_no
      varchar status
      numeric total_amount
      char currency
      jsonb snapshot
    }
    QUOTE_DECISION {
      uuid id PK
      uuid workspace_id FK
      uuid quote_version_id UK
      varchar decision
      text message
      timestamptz decided_at
    }
    COMMERCIAL_BASELINE {
      uuid id PK
      uuid workspace_id FK
      uuid project_id FK
      int version_no
      numeric total_amount
      char currency
      date delivery_date
      varchar status
    }
    BASELINE_ITEM {
      uuid id PK
      uuid workspace_id FK
      uuid baseline_id FK
      varchar item_type
      uuid source_version_id
      jsonb content
    }
    DELIVERY_DRAFT {
      uuid id PK
      uuid workspace_id FK
      uuid milestone_id FK
      varchar status
      bigint version
    }
    DELIVERY_VERSION {
      uuid id PK
      uuid workspace_id FK
      uuid delivery_draft_id FK
      uuid milestone_id FK
      int version_no
      varchar status
      jsonb snapshot
    }
    ACCEPTANCE_DECISION {
      uuid id PK
      uuid workspace_id FK
      uuid delivery_version_id UK
      varchar decision
      text reason
      timestamptz decided_at
    }
    CHANGE_DRAFT {
      uuid id PK
      uuid workspace_id FK
      uuid project_id FK
      varchar status
      bigint version
    }
    CHANGE_VERSION {
      uuid id PK
      uuid workspace_id FK
      uuid change_draft_id FK
      uuid project_id FK
      uuid base_baseline_id FK
      int version_no
      varchar status
      numeric amount_delta
      int schedule_delta_days
      jsonb snapshot
    }
    CHANGE_DECISION {
      uuid id PK
      uuid workspace_id FK
      uuid change_version_id UK
      varchar decision
      text message
      timestamptz decided_at
    }
    RECEIVABLE {
      uuid id PK
      uuid workspace_id FK
      uuid project_id FK
      varchar source_type
      uuid source_id
      numeric amount_due
      numeric amount_paid
      char currency
      date due_date
      varchar status
      bigint version
    }
    PAYMENT_RECORD {
      uuid id PK
      uuid workspace_id FK
      uuid receivable_id FK
      numeric amount
      char currency
      varchar status
      timestamptz paid_at
    }
    REMINDER {
      uuid id PK
      uuid workspace_id FK
      uuid receivable_id FK
      varchar status
      timestamptz requested_at
      timestamptz sent_at
    }
    FILE_ASSET {
      uuid id PK
      uuid workspace_id FK
      varchar object_key UK
      varchar status
      bigint size_bytes
      varchar checksum
    }
    FILE_REFERENCE {
      uuid id PK
      uuid workspace_id FK
      uuid file_asset_id FK
      uuid delivery_version_id FK
      varchar reference_type
    }
    PUBLIC_LINK {
      uuid id PK
      uuid workspace_id FK
      varchar object_type
      uuid object_id
      varchar token_hash UK
      varchar status
      timestamptz expires_at
    }
    PUBLIC_SESSION {
      uuid id PK
      uuid workspace_id FK
      uuid public_link_id FK
      varchar session_token_hash UK
      varchar status
      timestamptz expires_at
    }
    PUBLIC_ACCESS_LOG {
      uuid id PK
      uuid workspace_id FK
      uuid public_link_id FK
      varchar action
      varchar result
      timestamptz occurred_at
    }
    IDEMPOTENCY_RECORD {
      uuid id PK
      uuid workspace_id FK
      varchar scope
      varchar operation
      varchar idempotency_key
      varchar request_hash
      varchar status
      jsonb response_snapshot
    }
    EVENT_PUBLICATION {
      uuid id PK
      uuid workspace_id FK
      varchar event_type
      varchar aggregate_type
      uuid aggregate_id
      varchar status
      timestamptz occurred_at
    }
    EMAIL_TASK {
      uuid id PK
      uuid workspace_id FK
      uuid event_publication_id FK
      varchar status
      timestamptz next_attempt_at
    }
    EMAIL_ATTEMPT {
      uuid id PK
      uuid workspace_id FK
      uuid email_task_id FK
      int attempt_no
      varchar result
      timestamptz attempted_at
    }
```

## 4. 组合外键示例

```sql
ALTER TABLE milestone
  ADD CONSTRAINT fk_milestone_project_workspace
  FOREIGN KEY (workspace_id, project_id)
  REFERENCES project (workspace_id, id);

ALTER TABLE delivery_version
  ADD CONSTRAINT fk_delivery_version_milestone_workspace
  FOREIGN KEY (workspace_id, milestone_id)
  REFERENCES milestone (workspace_id, id);
```

## 5. 图模型说明

1. User 是全局身份；WorkspaceMember 决定用户在哪个租户中拥有什么角色。
2. Project 是商业闭环主容器，但报价、基线、交付、变更、应收分别拥有自己的聚合和状态机。
3. Project 的 `current_baseline_id` 是便捷指针，CommercialBaseline 历史链仍完整保留。
4. Task 和 Feedback 是可重建投影，ActivityLog 是不可变事实记录。
5. 所有公开链接绑定单一版本对象；文件公开下载必须通过 DeliveryVersion 的 FileReference 验证。
