package com.milestoneflow.task.domain.type;

/**
 * Task priority enum.
 *
 * <p>Three priority levels:
 * <ul>
 *   <li>{@code LOW} — low priority</li>
 *   <li>{@code MEDIUM} — default priority</li>
 *   <li>{@code HIGH} — high priority</li>
 * </ul>
 */
public enum TaskPriority {
    LOW,
    MEDIUM,
    HIGH
}
